{
  "name": "agilibo_protractor_jasmine",
  "version": "1.0.0",
  "description": "smoke",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "pree2e": "webdriver-manager update --standalone false --gecko false",
    "e2e": "protractor",
    "posttest": "allure generate test-report/allure-results --clean -o test-report/allure-report || true"
  },
  "author": "jihan",
  "license": "ISC",
  "dependencies": {
    "jasmine-allure-reporter": "^1.0.2",
    "package": "^1.0.1"
  }
}
